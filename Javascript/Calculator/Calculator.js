module.exports = {
    sum : (a,b) => a + b,
    different : (a,b) => a-b,
    mutiply : (a,b) => a*b,
    divide : (a,b) => a/b,
    pow : (a,b) => Math.pow(a,b),
    sqrt : (a,b) => Math.sqrt(a,b)
}